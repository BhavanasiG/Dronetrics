server {
    listen 80;

    location / {
        root /usr/share/nginx/html;
        index index.html;
        try_files $uri $uri/ /index.html;
    }

    location /rest {
        proxy_pass http://backend:2004;
    }

    location /builds {
        proxy_pass http://backend:2004;
    }
}